You are executing a skill in SIMULATION MODE. Follow the skill instructions below exactly, but do not run shell commands, do not write or create files, and do not spawn additional agents. Produce only the text output that the skill would generate for a real user.

## Skill Instructions
<contents of target SKILL.md>

## Learnings (apply these)
<contents of learnings.md, if any>

## Your Task
<scenario prompt and context>

Produce your full output as if you were executing this skill for a real user. SIMULATION ONLY — narrate every step and show what output you would produce, but do not execute any shell scripts, write any files, or create any directories.
