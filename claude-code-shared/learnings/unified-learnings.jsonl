{"type":"attribution","reported_by":"debug","improves":"to-seed","improves_type":"skill","cause":"other","cause_label":"version_double_encoded_in_filename","problem":"A new physical file `learning-schema-v2.json` was created instead of bumping `learning-schema.json` in place. The JSON Schema already carries a `schema_version` const field (v1=\"1\", v2=\"2\") that fully tracks the version internally. A version-suffixed filename only earns its keep when old v1 data must keep validating against v1 while new data uses v2 — but the learning store had zero data. The result: `learning-schema.json` is orphaned (no live code references it), `$id: learning-schema-v2` encodes the version redundantly, `learning-contract.md` now documents a deleted system, and two schema files must be maintained where one would do.","why_missed":"The seed framed the work as creating a new v2 superset schema artifact (\"one v2 superset schema\", \"Unified learning record (superset schema, schema_version 2)\") rather than bumping the existing file in place. It noted the zero-data condition as making unification \"near-free\" but did not flag that a version-suffixed filename redundantly double-encodes what the internal `schema_version` const already tracks, nor that with zero v1 data there is no v1-coexistence requirement that would justify a separate physical file. to-tasks faithfully transcribed that framing into a literal path (`learning-schema-v2.json`) in T-0026, and run-tasks executed it — both blameless downstream.","lesson":"When a design decision involves versioning a schema or contract that already carries an internal version field (e.g. `schema_version` const), to-seed should surface whether a new physical filename is actually necessary. The test: does old-version data need to keep validating against the old schema while new data uses the new schema? If the answer is no (especially when the store has zero data), an in-place rename or edit is correct, and a version-suffixed filename only adds a second encoding of the version with no benefit.","fix":"Add a prompt to to-seed's design-review checklist: 'If you are versioning a schema or contract file, check whether the schema already has an internal version field. If yes, and if there is no live old-version data requiring continued validation against the old file, recommend bumping in place rather than creating a new version-suffixed filename.'","evidence":[{"source":"artifact","ref":"docs/seeds/20260605-1358-retro-attribution-learning.json","quote":"Unify three learning forks (existing skill-learning, future agent-learning, this retroactive-attribution) into ONE system: one v2 superset schema, one physical append-only log. The existing per-skill-file layout is replaced and the live skill-learning system is rewired into the unified log. Done now while there is zero data, because convergence is near-free today and a migration project later."},{"source":"artifact","ref":"docs/seeds/20260605-1358-retro-attribution-learning.json","quote":"Unified learning record (superset schema, schema_version 2) fields: type (self|attribution), reported_by (slug that found/logged it), improves (slug the learning targets, or null), improves_type (skill|agent|process|contract), cause (open vocab string), cause_label (required iff cause == 'other'), problem, why_missed, lesson, fix, evidence (array of {source: transcript|artifact, ref, quote}), trace ({seed?, tasks?, task_id?, pr?, branch?}, attribution-only), confidence (confirmed|candidate), status (captured|applied, optional acted-on flag for the future Layer 2). schema_version, timestamp, and id are server-injected by the writer."},{"source":"artifact","ref":"docs/seeds/20260605-1358-retro-attribution-learning.json","quote":"Unification requires migrating the live skill-learning system (24 tail blocks, capture-learning agent, log-learning.py, learning-schema.json, learnings/ dir). Cheap now with zero data, expensive once data and the agent-learning fork accrue."},{"source":"artifact","ref":"docs/tasks/20260605-1501-retro-attribution-learning.json","quote":"Create the v2 superset learning schema at `claude-code-shared/contracts/learning-schema-v2.json`."}],"trace":{"seed":"docs/seeds/20260605-1358-retro-attribution-learning.json","tasks":"docs/tasks/20260605-1501-retro-attribution-learning.json","task_id":"T-0026","branch":"feat/retro-attribution-learning"},"confidence":"confirmed","schema_version":"2","id":"ac84212c-930a-423b-a319-347449f554fb","timestamp":"2026-06-05T22:44:50Z"}
{"type":"self","reported_by":"debug","improves":"debug","improves_type":"skill","cause":"general_best_practice","cause_label":null,"problem":"Test problem.","why_missed":"Not documented.","lesson":"Document it.","fix":null,"evidence":[{"source":"transcript","ref":"/tmp/t.md","quote":"test quote"}],"confidence":"confirmed","schema_version":"2","id":"d4ed20b2-36a3-4256-886e-ae0fe580fc54","timestamp":"2026-06-05T23:01:03Z"}
