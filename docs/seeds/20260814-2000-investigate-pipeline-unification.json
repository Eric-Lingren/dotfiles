{
  "schema_version": "4",
  "producer": "to-seed",
  "source": {"type": "session", "ref": null},
  "title": "Unify investigate/answer pipeline into single entry command",
  "slug": "investigate-pipeline-unification",
  "created": "2026-08-14T20:00:00Z",
  "iteration": 1,
  "summary": "The investigate/answer skill pipeline has three compounding bugs: two separate doorways when one is expected, no guard preventing the answer-composer from voicing un-investigated prose, and an investigator that answers only the literal claim without mechanism or edge cases. All branches resolved: unify into a single /investigate command with --out modes (default: slack voiced), delete /answer entirely, add an input guard forcing fresh investigation unless the input is schema-valid investigation-result JSON, and enable bounded expansion in the investigator (always-on, relevance-gated, every adjacent fact must carry a blob-URL citation).",
  "decisions": [
    "answer-composer stays a pure voicer and adds zero content beyond the investigation-result. Depth lives upstream in the investigator.",
    "/investigate always runs the investigator. Only a schema-valid investigation-result JSON may skip to voicing. Raw prose always triggers fresh investigation from the codebase. The composer never sees un-verified input.",
    "The investigator uses bounded expansion: it answers the literal claim plus a relevance-gated set of adjacent material facts. Every adjacent fact carries a blob-URL citation. Facts that prove not useful are dropped. No bloat obligation.",
    "/investigate is the single unified entry command with --out modes. Default output is slack-voiced. /answer skill is deleted entirely with no alias.",
    "v1 --out modes are raw and slack only. pr and linear modes are deferred until the adapters are real and voice profiles exist.",
    "Bounded expansion is always-on in the investigator agent. Machine callers (pr-revise, pr-code-review) also get depth. This may surface additional code review signals.",
    "The answer-composer agent is retained as the slack voicer. Only the /answer skill file is deleted."
  ],
  "open_threads": [],
  "disposed_threads": [],
  "status": "ready",
  "problem_statement": "The investigate/answer skill pipeline has three compounding bugs: (1) two separate skills (/investigate and /answer) when the user expects one unified command that does both investigation and voicing; (2) no guard preventing the answer-composer from voicing un-investigated prose, producing unverified output that looks authoritative; (3) the investigator answers only the literal claim without mechanism, edge cases, or adjacent material facts, producing shallow answers.",
  "solution": "Unify /investigate as the single entry command. Add --out modes (raw | slack) with slack as the default. Delete /answer skill entirely. Add an input guard: raw prose always triggers fresh investigation; only schema-valid investigation-result JSON may skip to voicing. Enable bounded expansion in the investigator (always-on): answer the literal claim plus relevance-gated adjacent facts, each with a blob-URL citation, dropping any that prove not useful.",
  "implementation_decisions": [
    "Delete /answer skill file. Update skill-pipeline.json, model-tiers.json, and the dispatch-tasks routing table (added in commit e736f45) to remove /answer references.",
    "Add --out flag parsing to /investigate SKILL.md. Default: slack. Accepted values: raw, slack.",
    "Add input guard to /investigate: detect schema-valid investigation-result JSON vs. raw prose. JSON path skips to answer-composer. Prose path always spawns fresh investigator.",
    "Update investigator agent to implement bounded expansion: after answering the literal claim, identify a capped, relevance-gated set of adjacent material facts the asker likely needs. Investigate each. Include only those with supporting evidence and a blob-URL citation. Drop any that prove uninformative. The specific cap count is unspecified and left to implementation.",
    "Blob-URL citation construction in investigator-code is already in place from commit 3c31398. No change needed.",
    "answer-composer agent is retained unchanged. It is now the voicing layer called exclusively from /investigate --out slack."
  ],
  "risks_and_tradeoffs": [
    "Always-on bounded expansion increases investigator token usage and latency. Machine callers (pr-revise, pr-code-review) pay this cost even for narrow literal claim checks. Accepted because depth may surface additional review signals.",
    "Deleting /answer removes a named command that may appear in user docs, bookmarks, or team instructions. No alias is intentional. Existing references will need updating.",
    "Bounded expansion relies on the investigator's judgment about relevance. A poorly calibrated heuristic could include bloat or miss material facts. The relevance-gate (drop if not useful) is a behavioral instruction, not a structural enforcement."
  ],
  "next_action": "Run /to-tasks to break this into implementable tasks",
  "verification": {
    "iteration": 1,
    "personas": ["grounding", "accuracy", "completeness", "coherence"],
    "refutations_upheld": 1,
    "refutations_screened": 2,
    "clean": false,
    "status": "verified"
  }
}
