{
  "schema_version": "4",
  "producer": "to-seed",
  "source": { "type": "session", "ref": null },
  "title": "Enrich triage task exports with context and backlinks",
  "slug": "enrich-triage-exports-with-context-backlinks",
  "created": "2026-06-06T15:50:00Z",
  "status": "ready",
  "iteration": 1,
  "summary": "After exporting T-0056/T-0057/T-0058 as GitHub Issues, the issues were evaluated as bare-bones stubs. A grill session resolved how triage task exports should be enriched. Triage issues are intentional stubs, not specs — they are enriched when re-ingested via a grill session. The to-tasks skill is responsible for embedding per-item context and dedicated backlink fields (seed_ref, task_ref) on triage task items. export-tasks renders the backlink footer only when wall != 'corporate'. All decisions resolved; seed is ready.",
  "problem_statement": "Triage tasks exported to GitHub Issues or Notion carried only the deferred item text, losing the surrounding context from the grill session. When these items are re-ingested in a future session, that context is gone unless it was preserved at export time.",
  "solution": "to-tasks populates per-item context (from deferred[].context in the seed) and dedicated backlink fields (seed_ref, task_ref as filename-only strings) on each triage task item. export-tasks renders a backlink footer block in the issue/page body when wall != 'corporate'. The description stays clean; the backlink is a separate dedicated field.",
  "decisions": [
    "Exported triage issues are intentional stubs, not specs. Enrichment happens when they are re-ingested via a grill session.",
    "to-tasks is responsible for enriching triage task descriptions with per-item context and backlink fields.",
    "Only the per-item deferred[].context field is included in the triage task description — not the session summary. The session summary would duplicate across all items from the same session.",
    "Backlinks use filename-only references (no directory prefix). This makes them path-agnostic and archival-safe regardless of future directory reorganization.",
    "Triage task items carry two new dedicated fields: seed_ref (seed filename, string) and task_ref (task file filename, string). These are separate from the description field.",
    "export-tasks renders a backlink footer block (seed: <filename>, task: <filename>, task_id: <id>) in the issue/page body only when wall != 'corporate'.",
    "Corporate-wall items receive no backlinks. Backlinks are suppressed for corporate-domain work to avoid exposing personal context in corporate systems.",
    "source.ref in task files switches to filename-only convention, matching the backlink standard and preparing for the same archival moves.",
    "Condensed or merged seed files carry a condensed_from: [...] field listing the original filenames as aliases, preserving archival fidelity.",
    "grill-me session-end protocol: print a plain text recommendation instead of using AskUserQuestion, which hard-blocks the terminal UI."
  ],
  "open_threads": [],
  "disposed_threads": [],
  "implementation_decisions": [
    "task-schema.json gains two optional string fields on task items: seed_ref and task_ref.",
    "to-tasks: when emitting a triage task from a deferred[] item, set description = deferred.text + (deferred.context if present), set seed_ref = seed filename (basename only), set task_ref = output task file filename (basename only).",
    "export-tasks: after resolving the adapter for a triage item, check wall value from task-routing.json. If wall != 'corporate', append the backlink footer block to the issue/page body: '---\\nseed: <seed_ref>\\ntask: <task_ref>\\ntask_id: <id>'.",
    "Condensed seed files: add condensed_from: [<filename>, ...] as an optional field in the seed schema to list original filenames when multiple seeds are merged.",
    "source.ref in task files: to-tasks writes only the basename (e.g. '20260606-0118-dispatch-tasks-pipeline.json'), not a relative path."
  ],
  "deferred": [
    {
      "text": "Corporate triage item enrichment and backlink strategy",
      "rationale": "The user deferred this conversation explicitly. Corporate items have different context-sharing constraints.",
      "context": "Personal-wall items now carry seed_ref + task_ref backlinks and per-item context. Corporate-wall items are stripped of backlinks at export time. The open question is whether corporate items should receive any enrichment at all, or whether a separate enrichment path exists (e.g. Linear description fields populated differently).",
      "source_seed": null
    },
    {
      "text": "clean-scaffolding archival structure",
      "rationale": "The user wants to modify clean-scaffolding to archive rather than delete, but the archival directory structure is not yet decided.",
      "context": "Currently clean-scaffolding deletes seeds, tasks, handoffs. With backlinks now pointing to filenames, archival moves are safe (filename-only refs resolve via find). The user is considering a Django-migration-style condensation approach but has not settled the structure. condensed_from field in seed schema is a prerequisite for this.",
      "source_seed": null
    }
  ],
  "out_of_scope": [
    {
      "text": "Fully speccing triage issues at export time",
      "rationale": "Triage issues are intentional stubs. Speccing them at export time would be premature — they haven't been grilled for the underlying work yet."
    },
    {
      "text": "Including session summary in triage task descriptions",
      "rationale": "Session summary is shared across all deferred items from a session. Including it would duplicate the same boilerplate in every issue from that session."
    },
    {
      "text": "Embedding the backlink block inside the description field",
      "rationale": "Embedding in description requires fragile text surgery to strip for corporate items. Dedicated fields (seed_ref, task_ref) allow clean conditional omission by export-tasks."
    },
    {
      "text": "Including the full decisions[] array from the seed in triage issue body",
      "rationale": "Decisions array could be 15-20 items. Including all would be noisy and mostly irrelevant to any specific deferred item. The backlink provides lazy-load access to the full context on demand."
    }
  ],
  "risks_and_tradeoffs": [
    "Filename-only refs require find-by-name resolution at consumption time. If two seeds share the same filename (should be impossible with YYYYMMDD-HHMM-slug naming), resolution is ambiguous.",
    "condensed_from aliases require the consuming tool to check aliases when a direct filename match fails. This is a lookup convention, not enforced by schema.",
    "wall != 'corporate' condition for backlinks includes project-wall items (e.g. spawned-sapien). Whether project-wall items should also be backlink-suppressed was not discussed in this session and may need revisiting."
  ],
  "next_action": "Run /to-tasks to generate implementation tasks for the to-tasks and export-tasks enrichment changes.",
  "verification": {
    "iteration": 1,
    "personas": ["grounding", "accuracy", "completeness", "coherence"],
    "refutations_upheld": 0,
    "refutations_screened": 2,
    "clean": true,
    "status": "verified"
  }
}
