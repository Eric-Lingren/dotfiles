{
  "schema_version": "2",
  "producer": "to-tasks",
  "source": { "type": "seed", "ref": "20260709-1312-dispatch-execution-isolation.json" },
  "generated_at": "2026-07-09T20:41:52Z",
  "branching": {
    "strategy": "single",
    "branch": "main"
  },
  "tasks": [
    {
      "id": "T-0070",
      "title": "Extend task-schema with receipt fields and run statuses",
      "type": "AFK",
      "description": "Add the per-task execution receipt to task-schema.json. Add four optional properties to the task item: summary (string), files_touched (array of strings), tests (object capturing passed/failed counts), and log_path (string pointing at the trace log). None go into the task-item required[] array. Keep additionalProperties:false on the task item. Do NOT bump schema_version (stays '2'; the change is backward-compatible additive). Also extend the task-item status enum with two new values the AFK loop needs: 'failed' (a build-runner task that did not pass) and 'deferred_hitl' (a HITL task skipped for a later interactive pass). Characterization first: capture that existing task files without the new fields still validate, then make the change keeping that true.",
      "acceptance_criteria": [
        "task-schema.json task item exposes summary, files_touched, tests, log_path as optional properties (absent from the item required[] array).",
        "task-item status enum includes failed and deferred_hitl in addition to the existing not_started, in_progress, done, merged, blocked.",
        "additionalProperties:false is retained on the task item and schema_version const stays '2' (no bump).",
        "Verification via validate-schema.sh: a sample task WITH the four new fields and a failed/deferred_hitl status validates, and an existing task WITHOUT the new fields still validates (backward compatible)."
      ],
      "blocked_by": [],
      "status": "done",
      "branch": null,
      "pr": "gh pr create --title \"Extend task-schema with receipt fields and run statuses\" --body \"Adds summary/files_touched/tests/log_path optional fields and failed/deferred_hitl statuses to task-schema.json task items. Backward compatible, schema_version stays 2.\"",
      "task_type": "code",
      "deliverable": "code"
    },
    {
      "id": "T-0071",
      "title": "Create build-runner executor agent and register at tier T3",
      "type": "AFK",
      "description": "Create a new agent build-runner (claude-code-shared/agents/build-runner.md) that executes exactly ONE task end-to-end: seed and run the /tdd red-green-refactor cycle for that single task, spawn lint-runner and test-runner, and spawn browser-checker when the task carries browser_verify. It writes the FULL execution trace to docs/tasks/.logs/<taskfile-basename>/<task_id>.md and returns a compact receipt carrying status, summary, files_touched, tests (passed/failed), pr, log_path, and any follow_ups. It must never loop over multiple tasks; one task per spawn. Register it by adding \"build-runner\": \"T3\" to the model-tiers.json agents map, then run sync-model-tiers.py --apply so model sonnet is stamped into the agent .md and the registry is updated.",
      "acceptance_criteria": [
        "claude-code-shared/agents/build-runner.md exists and runs exactly one task's TDD cycle, spawning lint-runner/test-runner and browser-checker (only when browser_verify is present).",
        "build-runner writes the full trace to docs/tasks/.logs/<taskfile-basename>/<task_id>.md and returns a compact receipt with status, summary, files_touched, tests, pr, log_path, follow_ups.",
        "model-tiers.json agents map contains build-runner:T3 and sync-model-tiers.py --apply has been run.",
        "Verification: after sync-model-tiers.py --apply, build-runner.md frontmatter and the agent registry both show model sonnet."
      ],
      "blocked_by": ["T-0070"],
      "status": "done",
      "branch": null,
      "pr": "gh pr create --title \"Create build-runner executor agent and register at T3\" --body \"Adds claude-code-shared/agents/build-runner.md, a single-task TDD executor that spawns lint-runner/test-runner/browser-checker and writes a full trace to docs/tasks/.logs/, returning a compact receipt. Registers build-runner in model-tiers.json and agents/registry.json at T3 (sonnet); sync-model-tiers.py --apply confirms model sonnet in both.\"",
      "task_type": "code",
      "deliverable": "code"
    },
    {
      "id": "T-0072",
      "title": "Rewrite build-code as per-task orchestrator loop",
      "type": "AFK",
      "description": "Replace build-code's inline /tdd execution with a per-task orchestrator loop that stays in the main thread and holds only compact receipts. Characterization first: before restructuring, capture build-code's current control flow (blocker checks, status writeback, pr writeback, follow_ups append) so the new loop preserves that behavior. Then: on start, build a shared context brief ONCE via the context-loader agent. Loop: pick the next not_started task whose blocked_by are all done or merged, spawn ONE build-runner for it, collect the receipt, write the receipt fields (status, summary, files_touched, tests, log_path, follow_ups) back into the task JSON, thread each completed receipt forward as a breadcrumb into subsequent build-runner spawns, and repeat. build-code must never hold the full TDD traces, only the receipts.",
      "acceptance_criteria": [
        "build-code no longer runs /tdd inline; it spawns exactly one build-runner per task and retains only receipts in the main thread.",
        "A shared brief is built once via context-loader at loop start and passed to each build-runner; each completed receipt is threaded forward as a breadcrumb.",
        "Receipt fields are written back into the task JSON item after each build-runner returns; blocked_by gating selects the next runnable task.",
        "Verification: a manual dispatch run of a multi-task all-AFK file completes with each task isolated, receipts and log_path written into the JSON, and traces written under docs/tasks/.logs/."
      ],
      "blocked_by": ["T-0071"],
      "status": "done",
      "branch": null,
      "pr": "gh pr create --title \"Rewrite build-code as per-task orchestrator loop\" --body \"build-code no longer runs /tdd inline. It builds a shared context brief once via context-loader, then spawns exactly one build-runner per task, writing back the returned receipt (status, summary, files_touched, tests, log_path) and threading each completed receipt forward as a breadcrumb. build-code retains only receipts in the main thread; full traces live under docs/tasks/.logs/. Note: could not live-fire a build-runner spawn in this session because the harness's agent registry loads once at session start and does not pick up a newly created agent file mid-session; verified structurally (frontmatter, registry entries, control-flow review) instead. Recommend a smoke run after restarting the session.\"",
      "task_type": "code",
      "deliverable": "code"
    },
    {
      "id": "T-0073",
      "title": "Add AFK obstacle policy and end-of-run report to build-code",
      "type": "AFK",
      "description": "Extend the build-code loop with obstacle handling and a final report. A leaf task failure (no other task lists it in blocked_by) marks the task status failed, skips it, and continues. A blocker task failure (at least one task lists it in blocked_by) halts the whole run; the report frames the halt as a scoping signal recommending re-grill or re-seed, not a blind retry. A HITL-type task is marked deferred_hitl and the loop continues independent work without halting on it. At the end, emit a consolidated report listing done, failed, deferred_hitl, and blocked tasks, each with its log_path. Resumption is free via task JSON status: re-invoking the loop picks up not_started and failed tasks.",
      "acceptance_criteria": [
        "Leaf task failure sets status failed, skips, and continues; blocker task failure halts the whole run with a report framing it as a scoping signal (re-grill/re-seed).",
        "A HITL-type task is marked deferred_hitl and the loop continues independent work without halting.",
        "End-of-run report lists done/failed/deferred_hitl/blocked tasks, each with its log_path; re-invoking resumes not_started and failed tasks.",
        "Verification: manual runs injecting (1) a leaf failure, (2) a blocker failure, and (3) a HITL task each exercise the correct policy branch."
      ],
      "blocked_by": ["T-0072"],
      "status": "done",
      "branch": null,
      "pr": "gh pr create --title \"Add AFK obstacle policy and end-of-run report to build-code\" --body \"Leaf task failures set status failed, skip, and continue; blocker task failures halt the run with a report framed as a scoping signal recommending re-grill/re-seed instead of blind retry. HITL tasks are marked deferred_hitl and never halt independent work. The queue-builder now resumes not_started and failed tasks on re-invoke. End-of-run report lists done/failed/deferred_hitl/blocked with each task's log_path. Note: same session constraint as T-0072, agent-spawn branches were verified structurally via control-flow review rather than a live multi-outcome run; recommend a smoke run after restarting the session to exercise leaf-failure, blocker-failure, and HITL branches live.\"",
      "task_type": "code",
      "deliverable": "code"
    },
    {
      "id": "T-0074",
      "title": "Clean trace logs in clean-scaffolding",
      "type": "AFK",
      "description": "Extend the clean-scaffolding skill to delete docs/tasks/.logs trace artifacts, treating them like browser-checks (delete, do not archive). Archiving of seeds, PRDs, tasks, and handoffs is unchanged. Characterization first: capture clean-scaffolding's current archive-vs-delete behavior so only the new .logs deletion is added.",
      "acceptance_criteria": [
        "clean-scaffolding deletes docs/tasks/.logs trace artifacts without archiving, matching the browser-checks handling.",
        "Existing archive behavior for seeds, PRDs, tasks, and handoffs is unchanged.",
        "Verification: running clean-scaffolding on a tree containing docs/tasks/.logs removes the logs and leaves the docs/archive contents intact."
      ],
      "blocked_by": [],
      "status": "done",
      "branch": null,
      "pr": "gh pr create --title \"Clean trace logs in clean-scaffolding\" --body \"Extends clean-scaffolding to delete docs/tasks/.logs trace artifacts as a delete-set (no archiving), matching browser-checks handling. Existing archive behavior unchanged.\"",
      "task_type": "code",
      "deliverable": "code"
    },
    {
      "id": "T-0075",
      "title": "Fix stale run-tasks references in helper runner agents",
      "type": "AFK",
      "description": "Update the 'spawned by run-tasks' references in the lint-runner, test-runner, and browser-checker agent docs to point at build-runner, since run-tasks is the former name of dispatch-tasks and these helpers are now spawned by the build-runner executor. Update e2e-runner's spawner note to build-runner as well if it references run-tasks.",
      "acceptance_criteria": [
        "lint-runner, test-runner, and browser-checker docs reference build-runner as their spawner, not run-tasks.",
        "Verification: grep for 'spawned by run-tasks' across claude-code-shared/agents returns no stale results."
      ],
      "blocked_by": ["T-0071"],
      "status": "done",
      "branch": null,
      "pr": "gh pr create --title \"Fix stale run-tasks references in helper runner agents\" --body \"Updates browser-checker, lint-runner, and test-runner agent docs to reference build-runner as their spawner instead of the former run-tasks name. e2e-runner had no run-tasks reference to fix.\"",
      "task_type": "code",
      "deliverable": "code"
    }
  ],
  "follow_ups": []
}
