# T-0003: Build /sprout-seed skill

**Started:** 2026-08-19
**Task:** Build a new skill that chains to-tasks → build-code → pr-code-review into one unattended run.

---

## Step 1: Reading key files

Read: to-tasks/SKILL.md, build-code/SKILL.md, pr-code-review/SKILL.md, resources/model-tiers.json

Key findings:
- All three skills are tier T3 (sonnet, high effort) per model-tiers.json
- to-tasks outputs "docs created here: docs/tasks/<filename>" after writing task file
- build-code asks "Push and open a PR?" at end — needs auto-confirm override
- pr-code-review can take a PR URL and fetch the diff itself via gh pr diff

## Step 2: Created skill file

Created: /Users/eric/.dotfiles/claude-code-shared/skills/sprout-seed/SKILL.md

## Step 3: TDD structural verification

All 5 acceptance criteria verified against created file:
1. No HITL gates between subagent spawns — auto-confirm overrides in all three prompts. PASS
2. Agent tool used (not Skill tool) — explicit in architecture section and each step. PASS
3. Model tier table matches model-tiers.json — all three skills at T3 (sonnet/high). PASS
4. Step 4 summary includes PR URL and pr-code-review findings. PASS
5. File confirmed at ~/.dotfiles/claude-code-shared/skills/sprout-seed/SKILL.md. PASS

## Step 4: Runner validation gate

Tooling detection: language=unknown, lint=null, test=null — no runners to spawn.
Git status: sprout-seed/SKILL.md is new untracked file. build-code/SKILL.md and pr-code-review/SKILL.md show as modified (from T-0001/T-0002).

Runner verdict: pass (no tooling configured for this workspace).

## Closing Summary

Task T-0003 complete. Created /Users/eric/.dotfiles/claude-code-shared/skills/sprout-seed/SKILL.md
describing the sprout-seed pipeline (to-tasks → build-code → pr-code-review) with:
- Agent-per-skill architecture (fresh context, Agent tool not Skill tool)
- Correct T3 model tiers (sonnet/high) for all three subagents
- Unattended operation via override prompts (no HITL gates)
- Output summary with task file path, PR URL, and pr-code-review findings

