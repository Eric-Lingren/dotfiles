# T-0002 — Parallelize pr-code-review by dimension

**Started:** 2026-08-19

---

## Step 1: Read current SKILL.md

Read `/Users/eric/.dotfiles/claude-code-shared/skills/pr-code-review/SKILL.md` (308 lines).

Current structure:
- Steps 1, 1a, 1b: gather diff, resolve voice profile, derive line numbers
- Step 2: understand context
- Step 3, 3a: evaluate design (correctness/quality)
- Step 4, 4a: categorize and format findings
- Steps 5–8: TypeScript, style, design system, testing checklists
- Step 9: security checklist
- Step 10: pre-submit claim verification (investigator gate)

The current implementation is serial — all steps run sequentially on one agent. No dimension-based parallelism exists yet.

## Plan

Restructure the skill so that after setup (steps 1, 1a, 1b), five dimension agents are spawned in a single parallel batch. Each agent gets the same diff and context, plus a dimension-specific prompt. After all five complete, apply mechanical dedup, then pass deduped findings through the investigator gate (step 10), and emit output in the same contract shape.

Dimensions and models:
- correctness: sonnet (steps 2, 3, 3a, 4a guidance)
- security: sonnet (step 9)
- performance: haiku (subset of step 3)
- conventions: haiku (steps 5, 6, 7)
- test coverage: haiku (step 8)

## Step 2: Edit SKILL.md

Replacing steps 2-9 with a parallel dimension agent section + dedup step.
Files to edit: `claude-code-shared/skills/pr-code-review/SKILL.md`

### Changes made

1. **Removed** Steps 2–9 (serial review steps): Understand context, Evaluate design, Design smell baseline, Categorize findings, Format comments, TypeScript/React checklist, Style/formatting checklist, Design system checklist, Testing checklist, Security checklist.

2. **Added Step 2: Spawn dimension agents in parallel** — instructs spawning all 5 agents in a single parallel batch with:
   - Shared finding JSON format with `file`, `line`, `severity`, `dimension`, `description` fields
   - Correctness dimension prompt (sonnet): covers context reading, design holistically, Fowler code smells, tone
   - Security dimension prompt (sonnet): covers full security checklist (injection, auth, access control, crypto, error handling, supply chain)
   - Performance dimension prompt (haiku): covers N+1 queries, memoization, re-renders, event loop blocking, unbounded structures
   - Conventions dimension prompt (haiku): covers TypeScript/React, style/formatting, design system checklists
   - Test-coverage dimension prompt (haiku): covers test existence, query patterns, MSW, coverage gaps

3. **Added Step 3: Dedup findings** — mechanical merge algorithm: group by (file, line), keep max severity (bug > risk > q > nit > praise), concatenate descriptions with dimension prefixes.

4. **Renamed** Step 10 to Step 4 (investigator gate), updated scope text to use JSON text labels (`bug`, `risk`, `nit`, `q`) instead of emojis.

5. **Updated Output format** section to include severity-to-emoji mapping table so the rendering step is explicit.

### Git diff stat
`claude-code-shared/skills/pr-code-review/SKILL.md | 245 +++++-----------`
127 insertions, 118 deletions (net +9 lines).

## Result

DONE. No tests applicable (SKILL.md rewrite — no executable code). No lint runner applicable.

