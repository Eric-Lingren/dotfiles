# T-0001 — Parallelize build-code with wave-based task execution

**Start:** 2026-08-19

## Plan

Rewrite `build-code/SKILL.md` steps 3b and 4 to describe wave-based parallel execution:

- Wave computation: group tasks by blocked_by graph
- Each wave runs concurrently with `isolation: 'worktree'` per task
- Cap: 4 concurrent tasks per wave
- After each wave: sequential merge into shared branch; conflict = fail task, continue
- Prior-wave receipts passed forward; same-wave breadcrumbs dropped to avoid confusion

## Files to modify

- `~/.dotfiles/claude-code-shared/skills/build-code/SKILL.md` — steps 3b and 4

## Implementation

Reading SKILL.md confirmed:
- Step 3b: currently initializes context_brief and breadcrumb = []
- Step 4: sequential per-task loop with blocker check, HITL handling, build-runner spawn

Rewriting step 3b to add wave computation after context_brief setup.
Rewriting step 4 to describe wave-based parallel execution.

## Acceptance criteria validation

1. Wave computation from blocked_by graph: covered by new "Compute waves" section
2. Linear chain A->B->C = 3 waves of 1: covered by wave algorithm description
3. Fanout > 1.00 for independent tasks: covered by concurrent Agent spawning
4. Merge conflict fails only that task: explicitly called out in merge section
5. Cap of 4 concurrent: explicitly stated
6. Latency drop: enabled by concurrent execution description

## Changes applied

See diff below — rewrote steps 3b and 4 in SKILL.md.

## Result

PASS — SKILL.md updated with wave-based parallel execution. All acceptance criteria addressed by the spec.
