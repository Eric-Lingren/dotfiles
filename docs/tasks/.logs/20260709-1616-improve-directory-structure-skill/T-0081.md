# T-0081 — Author improve-directory-structure SKILL.md

Start: 2026-07-09

## Task
Write the improve-directory-structure skill: invokes the scanner to build the dependency
graph, reuses context-loader + grill-step pattern, hands off verified reorg plan via to-seed.
Must never execute file moves directly.


## Files authored
- claude-code-shared/skills/improve-directory-structure/SKILL.md
- claude-code-shared/skills/improve-directory-structure/resources/LITMUS.md

## Manual end-to-end verification (acceptance criterion 2)

Ran the newly-authored skill for real via the Skill tool against claude-code-shared itself.

1. Scope check: claude-code-shared matches the claude-tooling-repo shape (skills/*/SKILL.md,
   agents/registry.json, resources/model-tiers.json, skill-pipeline.json all present).
2. Spawned context-loader agent for real -- confirmed CONTEXT.md found, 8 vocabulary terms,
   no ADRs (docs/adr/ absent), 5 extra sources.
3. Ran: python3 scripts/scanner.py --extractor claude-tooling --out graph.json -> 101 nodes, 209 edges.
   Ran: python3 scripts/scanner.py --check -> FAIL: 1 pre-existing dangling reference
   (agents/registry.json's browser-checker entry lists stale consumer 'run-tasks'), 0 cycles.
   This is pre-existing debt, unrelated to this run, not fixed here (out of scope).
4. Ran: python3 scripts/dependency-graph-findings.py graph.json --out findings.json -> 139 findings.
5. Selected two concrete, cited candidates and declined two known-non-findings per LITMUS.md:
   - ACCEPT F0003 (CCP): agent:build-runner shared but sole-owner cluster 'build-code' -> co-locate.
   - ACCEPT F0009 (CRP): agent:browser-checker shared-bucket, single consumer 'debug' -> co-locate.
   - DECLINE: MODULARITY finding on cluster 'agents' -- deliberately-shared bucket, known non-finding.
   - DECLINE F0008 (CCP): scripts/scanner.py sole-consumer 'improve-directory-structure' -- artifact
     of the skill being brand new, not a real ownership signal (scanner.py is general-purpose by design).
6. Confirmed via `git status --short` that no file move/rename occurred -- only new untracked skill
   files, no renames.
7. Invoked /to-seed for real (Mode 1, live conversation). Branch check: gxcheck WARN (advisory,
   author-mismatch, non-blocking) on feat/improve-directory-structure. Auto-mode: continued on
   current branch rather than blocking on AskUserQuestion (no live human in this run).
   Ran filter-session-transcript.sh (228 in / 135 out lines). Wrote draft seed JSON. Spawned all
   4 adversary personas (grounding, accuracy, completeness, coherence) in parallel per spec.
