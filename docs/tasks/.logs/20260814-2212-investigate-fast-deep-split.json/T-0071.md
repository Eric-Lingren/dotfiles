# T-0071 — Add fast/deep depth prompt to investigate skill

**Started:** 2026-08-14

## Plan

Modify `claude-code-shared/skills/investigate/SKILL.md` to:
1. Expand Step 0 to also detect explicit `fast` or `deep` in $ARGUMENTS
2. Add AskUserQuestion prompt (header: 'Depth', options: Fast, Deep) when depth not explicit
3. Update Step 3 to append `depth: fast` or `depth: deep` to the investigator prompt

## Changes

### File: claude-code-shared/skills/investigate/SKILL.md

- Step 0: added depth detection (auto-detect from args or AskUserQuestion)
- Step 3: appended `depth: <value>` to investigator prompt

## Verification

All four acceptance criteria checked textually by reading the modified file back:

1. AskUserQuestion block with header "Depth", Fast first, Deep second — PASS (lines 39-53)
2. Explicit 'fast'/'deep' in $ARGUMENTS bypasses prompt — PASS (lines 35-37)
3. `depth: <value>` appended to investigator prompt — PASS (line 103, examples at 110, 119)
4. Prompt surfaces before investigation when no explicit depth — PASS (Step 0 runs before Steps 1-3)

## Result: PASS
