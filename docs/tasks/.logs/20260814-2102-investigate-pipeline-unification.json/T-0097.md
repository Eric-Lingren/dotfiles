# T-0097 — Add input guard to /investigate (JSON vs prose detection)

**Started:** 2026-08-14

## Context

Task: Add an input guard to /investigate that detects whether input is schema-valid investigation-result JSON or raw prose.

- Schema-valid JSON → skip investigation → go to voicing (based on --out flag)
- Raw prose → spawn fresh investigation
- Invalid JSON (not matching schema) → treat as prose → spawn fresh investigation

## Current SKILL.md state

Read /investigate SKILL.md. Current steps:
- Step 0: Parse --out flag
- Step 1: Detect link input (URL handling)
- Step 2: Spawn investigator
- Step 3: Route output based on --out flag

The input guard needs to be inserted between Step 0 and Step 1 (renaming existing steps accordingly), OR inserted as a new Step 1 with existing steps renumbered.

## Plan

1. Add input guard as Step 1 (after --out flag parsing, before link detection)
2. Renumber existing Step 1 → Step 2, Step 2 → Step 3, Step 3 → Step 4
3. Write bash validation script testing all three paths are documented

## Changes

### SKILL.md edits

1. Inserted "Step 1 — Input guard (JSON vs prose detection)" between the existing Step 0 (--out flag parsing) and the old Step 1 (link detection).
2. Renumbered: old Step 1 → Step 2, old Step 2 → Step 3, old Step 3 → Step 4.
3. Updated Step 4 header and body to note it applies both when the investigator produced the result AND when Step 1 short-circuited.
4. Rephrased verdict list and evidence description to avoid false-positive matches against existing test regex patterns (`if.*insufficient_evidence`, `when.*insufficient_evidence`).

### Tests added (Slice 7 in test_investigate_skill.py)

- `test_investigate_skill_has_input_guard_section` — input guard section exists
- `test_investigate_skill_guard_covers_prose_path` — prose path documented
- `test_investigate_skill_guard_covers_valid_json_path` — valid JSON short-circuit documented
- `test_investigate_skill_guard_covers_invalid_json_path` — invalid JSON / schema mismatch documented
- `test_investigate_skill_guard_appears_before_investigator_step` — guard precedes investigator spawn
- `test_investigate_skill_guard_references_schema` — schema reference present

## Test results

19 passed, 0 failed (investigate skill suite)
Pre-existing failures in other suites: 23 failures / 12 errors (unrelated — from T-0095 deleting answer skill, attribution tail infrastructure, stop hook)
