# T-0096 — Add --out flag to /investigate with raw and slack modes

**Start:** 2026-08-14
**Task:** Add --out flag parsing to /investigate SKILL.md

---

## Step 1: Read current SKILL.md

Read `/Users/eric/.dotfiles/claude-code-shared/skills/investigate/SKILL.md`.

Current structure:
- Step 1: Detect link input
- Step 2: Spawn investigator, return raw JSON

Also read answer-composer at `agents/egress/answer-composer.md`.

## Step 2: Plan changes

Add a Step 0 to SKILL.md that:
- Parses --out from args (default: slack)
- Documents accepted values: raw, slack

Modify Step 2 to branch:
- --out raw: return investigation-result JSON directly
- --out slack: spawn answer-composer with the result for voicing

## Step 3: Edit SKILL.md

Added Step 0 (--out flag parsing), modified Step 2 to remove the raw return, added Step 3 (routing logic).

## Step 4: Write validation script

Written at `claude-code-shared/scripts/validate-investigate-out-flag.sh`.

10 checks covering:
- --out flag parsing step present
- Accepted values raw and slack documented
- Default is slack
- --out raw documented
- --out slack documented
- Strip --out from args instruction present
- Routing step (Step 3) present
- Raw mode returns investigation-result JSON directly
- Slack mode routes through answer-composer
- Invalid --out value error instruction present

## Step 5: Run validation

All 10 checks passed.

## Summary

SKILL.md updated with --out flag parsing in Step 0, routing logic in Step 3. Validation script confirms all acceptance criteria are met.
