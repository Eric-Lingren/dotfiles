# T-0015 — Create sprout handoff metadata schema

**Started:** 2026-08-28

## Plan

Create `claude-code-shared/contracts/sprout-handoff-schema.json` with:
- phase (string, required)
- paths (array of strings, required)
- status (enum: success|failed|skipped, required)
- errors (array of strings, required, empty on success)

Include `examples[]` array with a valid conforming instance.
Validate via `validate-schema.sh --instance` with:
1. A valid instance (must pass)
2. An instance with invalid status value (must fail)

## Execution

### Step 1: Create sprout-handoff-schema.json

Writing schema to claude-code-shared/contracts/sprout-handoff-schema.json.

### Step 2: Run validate-schema.sh on the schema itself (examples[] validation)

### Step 3: Run --instance with invalid status to confirm rejection

## Results

### validate-schema.sh (examples[] validation)
```
OK: sprout-handoff-schema.json: 3 example(s) valid
```

### validate-schema.sh --instance with invalid status
```
ERROR: handoff-invalid.json: status: 'invalid-status-value' is not one of ['success', 'failed', 'skipped']
exit: 1
```

## Acceptance Criteria Check
- [x] sprout-handoff-schema.json validates as valid JSON Schema draft-07
- [x] A sample handoff JSON conforming to the schema passes validate-schema.sh
- [x] A handoff JSON with an invalid status value fails validate-schema.sh

## Status: DONE
