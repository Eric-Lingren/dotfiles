# T-0019 — Refactor sprout-seed orchestrator to file-based handoff

**Started:** 2026-08-28  
**Task:** Refactor the sprout-seed SKILL.md orchestrator to use file-based handoff instead of accumulating full subagent output.

---

## Step 1: Read and map current orchestrator

Read `/Users/eric/.dotfiles/.claude/worktrees/agent-ad309b8e1f410dd0c/claude-code-shared/skills/sprout-seed/SKILL.md` (199 lines).

### Subagent spawn points identified

| Step | Agent type | Spawn line | Current data flow |
|------|-----------|-----------|-------------------|
| Step 1 | to-tasks | ~L56 | Captures full output; parses `docs created here: docs/tasks/<filename>` line |
| Step 2 | build-code | ~L96 | Captures full output; parses `PR URL: <url>` line |
| Step 3 | pr-code-review | ~L144 | Captures full output (`Capture the agent's full findings output.`) |

### Dead/duplicate code identified

- **Line 133**: `git diff main...HEAD` — dead call; result captured but never used (pr-code-review uses `gh pr diff` instead)
- **Line 185**: `<full pr-code-review output>` — Step 4 embeds the full captured findings (duplicate embed)

### Phase handoff file mapping

Per sprout-handoff-schema.json (T-0015), each handoff has: `phase`, `paths`, `status`, `errors`.

- `phase-1-tasks.json`: paths = [task file path], status = success/failed
- `phase-2-build.json`: paths = [PR URL], status = success/failed  
- `phase-3-review.json`: paths = [findings file path], status = success/failed

---

## Step 2: Write refactored SKILL.md

File written to: `claude-code-shared/skills/sprout-seed/SKILL.md`

Changes made:
1. Architecture section updated: "Context flows only through handoff files written to docs/tasks/.sprout/ — the orchestrator never captures or stores a subagent's full return text."
2. Step 0: Added `mkdir -p docs/tasks/.sprout` 
3. Step 1 (to-tasks): Prompt now instructs agent to write `docs/tasks/.sprout/phase-1-tasks.json`. Orchestrator validates with jq instead of parsing agent output text.
4. Step 2 (build-code): Removed `git diff main...HEAD` block. Prompt now instructs agent to write `docs/tasks/.sprout/phase-2-build.json`. Orchestrator validates with jq.
5. Step 3 (pr-code-review): Removed "Capture the agent's full findings output." Prompt now instructs agent to write findings to `docs/tasks/.sprout/phase-3-review-findings.md` and handoff to `docs/tasks/.sprout/phase-3-review.json`.
6. Step 4: Removed `<full pr-code-review output>` embed. Summary now references the findings file path.
7. Error handling: Updated to reference handoff files.

---

## Step 3: Validation checks

```
git diff --name-only
claude-code-shared/skills/sprout-seed/SKILL.md
```

### Acceptance criteria

- [x] Orchestrator does not capture full Agent() return text — "Capture the agent's output" and "Capture the agent's full findings output" both removed; no variable captures agent return
- [x] `git diff main...HEAD` call removed — grep confirms absent
- [x] Duplicate pr-code-review findings embed removed — `<full pr-code-review output>` removed from Step 4
- [x] Each phase writes to `docs/tasks/.sprout/<phase>.json` — phase-1-tasks.json, phase-2-build.json, phase-3-review.json all referenced in prompts
- [x] Orchestrator validates handoff files against sprout-handoff-schema.json — schema path referenced in Architecture section and Step 1; jq validation commands present in each step

---

## Closing Summary

**Status:** done  
**Files touched:** `claude-code-shared/skills/sprout-seed/SKILL.md`  
**Tests:** skipped (markdown instruction file, no executable code; tooling manifest shows no test command)
