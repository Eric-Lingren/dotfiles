# T-0018: Add local_only mode to build-code

**Started:** 2026-08-28
**Status:** done

---

## Step 1: Read relevant files

Read `claude-code-shared/contracts/task-schema.json` — found top-level `properties` section with `source_branch`, `gate_override`, `follow_ups`. Identified insertion point for `local_only` before `source_branch`.

Read `claude-code-shared/skills/build-code/SKILL.md` — identified two key sections:
- Step 4b lines ~157-170: where build-runner agents are spawned and per-task data is passed.
- Step 7 lines ~286-332: "Offer to push and open a PR" — the push/PR logic.

---

## Step 2: Changes applied

### Change 1: task-schema.json — add `local_only` boolean

**File:** `claude-code-shared/contracts/task-schema.json`

Inserted new optional top-level property before `source_branch`:

```json
"local_only": {
  "type": "boolean",
  "description": "When true, build-code skips the push/PR step entirely after all tasks complete. Useful for in-place schema changes, config updates, or tasks that should not be pushed automatically. Optional — omit for normal push/PR behavior."
},
```

**JSON validation:** `python3 -c "import json; json.load(...)"` → `Valid JSON`

### Change 2: build-code SKILL.md — build-runner prompt instruction

**File:** `claude-code-shared/skills/build-code/SKILL.md`

Added after the "Pass per task:" list in Step 4b:

> **When `local_only: true` is set at the task file root**, include this explicit instruction in every build-runner prompt:
>
> "Do NOT push or create a PR. The task file has `local_only: true` — all changes must remain as local commits only."

### Change 3: build-code SKILL.md — skip push/PR in Step 7

**File:** `claude-code-shared/skills/build-code/SKILL.md`

Added at the top of Step 7 "Offer to push and open a PR":

> **Skip this step entirely if `local_only: true` is set at the task file root.** Do not ask the user about pushing, do not run gxpush, and do not open a PR. Print this note in place of the push prompt: `"local_only: true — push and PR skipped."` Then proceed directly to the learning-capture block.

---

## Step 3: Validation

- JSON valid: confirmed.
- No test suite present (tooling manifest shows no test runner for this workspace).
- Existing task files without `local_only` are unaffected — the field is optional with `additionalProperties: false` guarded by the schema.

---

## Summary

Added `local_only` optional boolean to task-schema.json top-level properties; updated build-code SKILL.md to (a) skip Step 7 push/PR entirely when `local_only: true`, and (b) inject an explicit no-push instruction into every build-runner agent prompt when `local_only` is set.
