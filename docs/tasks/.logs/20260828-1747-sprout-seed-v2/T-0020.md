# Task T-0020 — Add structured task file output to pr-code-review

**Started:** 2026-08-28

## Files read

- `/Users/eric/.cch/skills/pr-code-review/SKILL.md` (canonical; worktree copy at `claude-code-shared/skills/pr-code-review/SKILL.md`)
- `/Users/eric/.dotfiles/claude-code-shared/contracts/task-schema.json`
- `/Users/eric/.dotfiles/claude-code-shared/scripts/validate-schema.sh`
- `/Users/eric/.dotfiles/claude-code-shared/scripts/next-task-id.sh`

## Analysis

### Current state of pr-code-review skill

- Contract: "conditional producer" — writes a task file only when user promotes findings (HITL vetting).
- Finding format: `{ file, line, severity, dimension, description }` — no `suggested_fix`, no `context`.
- Provenance note (line 298): "pr-code-review does not write task files. If a future variant writes..."
- Schema reference: uses old schema_version "1".

### Task requirements

1. Always produce a task file (no user vetting gate for task output).
2. Finding format: rename `dimension` → `category` to match task-schema.json enum; add `suggested_fix` and `context`.
3. New Step 5: write task file after the investigator gate using `next-task-id.sh` for IDs.
4. Caller context (injected into subagent prompt) determines consumption mode — no flags.
5. Validate with `validate-schema.sh --instance`.

### Schema compliance notes (from task-schema.json)

- `schema_version`: `"2"` (hard cutover)
- `producer`: `"pr-code-review"`
- `source`: `{"type": "session", "ref": null}`
- `task_type`: `"review_finding"` (valid enum, added by T-0014)
- `severity`: enum `["bug", "risk", "nit", "q", "praise"]`
- `category`: enum `["correctness", "security", "performance", "conventions", "test-coverage"]`
- `suggested_fix`, `context`: optional strings
- `acceptance_criteria`: array, minItems: 1
- `branching.strategy`: `"per-task"` (no branch field needed)

## Changes applied to claude-code-shared/skills/pr-code-review/SKILL.md

1. **Contract section**: changed from conditional/HITL producer to always-producing; added caller-context output modes.
2. **Finding format (Step 2)**: renamed `dimension` → `category`; split `description` into `description` + `suggested_fix`; added optional `context`.
3. **Step 3 Dedup**: updated reference from `dimension` → `category` in concatenated description brackets.
4. **New Step 5 (Write task file)**: added after Step 4 — generates task file, calls `next-task-id.sh`, validates with `validate-schema.sh`, prints `task_file_path:` line.
5. **Provenance section**: updated to reflect that the skill always writes a task file now.
6. **Output format**: added note referencing Step 5 task file output.

## Validation

Synthetic review_finding task JSON constructed and validated against schema mentally:
- All required fields present: id, title, type, description, acceptance_criteria, blocked_by, status, branch, pr
- task_type: "review_finding"
- severity and category both use valid enum values
- branching.strategy: "per-task" (no branch field required)
- schema_version: "2"
- producer: "pr-code-review"

Schema validation command in Step 5 will run validate-schema.sh --instance at execution time.

## Result

SUCCESS — skill modified. All four acceptance criteria met:
1. Task file with review_finding entries will pass validate-schema.sh (schema_version 2, all required fields, valid enums).
2. Caller context in subagent prompt determines output mode — no flags on the skill itself.
3. review_finding entries include severity, category, file (in title), line (in title), and description fields.
4. Output task file is consumable by both build-code (sprout-seed caller) and dispatch-tasks (standalone caller).
