# T-0076 — Add 3-line header comments to existing .scripts/ files

**Start:** 2026-08-15
**Status:** in-progress

## Goal

Add standard 3-line header (name — one-liner / What: / When:) to the top of 9 target scripts in `.scripts/`. All files have shebangs, so header goes on lines 2-4.

## File inventory

| File | Pre-existing line-2 comment? |
|------|------------------------------|
| gxcheck | yes — `# gxcheck — read-only branch state reporter` |
| gxclean | yes — `# gxclean — on-demand merged branch cleanup` |
| gxmove | yes — `# gxmove — relocate uncommitted changes to another branch` |
| gxpush | yes — `# gxpush — preview-and-confirm push with flags` |
| gxsync | yes — `# gxsync — sync feature branch with base via merge` |
| gx-lib.sh | yes — `# gx-lib.sh — shared helpers for the gx git toolkit` |
| pr-commit | yes — one-liner + two extra Usage comment lines to replace |
| pr-desc | no header at all — blank line after shebang |
| gxpush-issue-link.test.sh | yes — one-liner + 3 extra comment lines to replace |

## Steps

1. Read all 9 files — done
2. Edit each file to add/complete the 3-line header — done
3. Write validation script at `.scripts/tests/test-headers.sh` — done
4. Run validation — done

## Validation output

```
=== test-headers.sh: 3-line header validation ===

PASS: gxcheck
PASS: gxclean
PASS: gxmove
PASS: gxpush
PASS: gxsync
PASS: gx-lib.sh
PASS: pr-commit
PASS: pr-desc
PASS: gxpush-issue-link.test.sh

===================================
Results: 9 passed, 0 failed
===================================
```

Exit code: 0

## Files touched

- `.scripts/gxcheck` — added What/When lines
- `.scripts/gxclean` — added What/When lines
- `.scripts/gxmove` — added What/When lines
- `.scripts/gxpush` — added What/When lines
- `.scripts/gxsync` — added What/When lines
- `.scripts/gx-lib.sh` — added What/When lines
- `.scripts/pr-commit` — replaced Usage comments with What/When lines
- `.scripts/pr-desc` — added full 3-line header (had no prior header)
- `.scripts/gxpush-issue-link.test.sh` — replaced extra comment lines with What/When lines
- `.scripts/tests/test-headers.sh` — new validation script

## Status: DONE
