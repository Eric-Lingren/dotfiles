# T-0073 — Add wt ls, wt rm, and bare/help subcommands

**Started:** 2026-08-15

## Plan

Extend `.scripts/worktree` with:
- `wt ls` — list worktrees for current repo
- `wt ls --all` — list all worktrees across all repos
- `wt rm <branch>` — remove worktree, safety checks, keep branch
- `wt rm --force` — bypass safety checks
- `wt -h` / `wt --help` — print help
- bare `wt` — help + ls

Also extend mock git and test suite.

## Changes log

### Files modified
- `.scripts/worktree` — added `print_help()`, `cmd_ls()`, `cmd_rm()`, dispatch block; bare invocation now shows help+ls
- `.scripts/tests/.mock-git` — added `-C` flag handling, `status --porcelain`, `log @{u}..`, `worktree remove/prune/list`; `worktree add` now creates `.git` file
- `.scripts/tests/test-worktree.sh` — added 38 new tests (ls, ls --all, rm success, rm dirty blocked, rm unpushed blocked, rm --force, bare, -h, --help)

### Test results
- All 63 tests pass (was 25 before new tests)
- 0 failures

## Summary

Worktree engine extended with wt ls, wt ls --all, wt rm (with dirty/unpushed safety checks and --force bypass), wt -h/--help, and bare wt (help+ls). Mock git updated to support -C flag and new git subcommands. Test suite expanded from 25 to 63 tests, all passing.
