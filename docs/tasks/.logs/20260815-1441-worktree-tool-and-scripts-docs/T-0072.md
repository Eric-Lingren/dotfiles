# T-0072 — Build worktree engine script and wt zsh function

**Start:** 2026-08-15
**Status:** IN PROGRESS

## Task

Create `~/.dotfiles/.scripts/worktree` bash engine script and `wt` zsh function.

4-step smart resolution:
1. Worktree folder already exists → print 'attaching to existing branch <name>' and cd in
2. Branch exists locally but no folder → create folder, git worktree add, cd in
3. Branch exists on origin remote only → fetch as tracking branch, create folder, git worktree add, cd in
4. Branch exists nowhere → [Y/n] confirmation, create from base (repo-policy.json GX_BASE_BRANCH), create folder, git worktree add, cd in

---

## Step 1: Read gx-lib.sh and scan zsh config structure

Read `/Users/eric/.dotfiles/.scripts/gx-lib.sh` — confirmed `gx_load_policy` sets `GX_BASE_BRANCH` from `repo-policy.json`; env var injection supported (test-friendly).

Scanned zsh config: single file at `~/.dotfiles/.zshrc`. No separate zsh/ directory.

## Step 2: Inspect existing files

All deliverables were already implemented before this task run:

- `.scripts/worktree` — bash engine, 81 lines, 3-line header comment, all 4 resolution cases, prefix warning, base override
- `.zshrc` — `function wt { ... }` at lines 92–99, delegates to `~/.dotfiles/.scripts/worktree "$@"`, cd's to printed path
- `.scripts/tests/test-worktree.sh` — 25 assertions covering all acceptance criteria
- `.scripts/tests/.mock-git` — mock git binary controlled via env vars (MOCK_LOCAL_BRANCHES, MOCK_REMOTE_BRANCHES, GIT_CALL_LOG, etc.)

## Step 3: Run tests

Command: `bash /Users/eric/.dotfiles/.scripts/tests/test-worktree.sh`

Output:
```
PASS: worktree script is executable
PASS: case1: exit code 0
PASS: case1: stdout is worktree path
PASS: case1: stderr says attaching
PASS: case2: exit code 0
PASS: case2: stdout is worktree path
PASS: case2: worktree directory was created
PASS: case3: exit code 0
PASS: case3: stdout is worktree path
PASS: case3: worktree directory was created after remote fetch
PASS: case4a: exit code 0
PASS: case4a: stdout is worktree path
PASS: case4a: stderr shows Y/n prompt
PASS: case4a: worktree directory was created
PASS: case4b: exit code 2 (declined)
PASS: case4b: stdout is empty when declined
PASS: prefix warn: stderr has WARN
PASS: prefix warn: mentions feat/fix/spike
PASS: good prefix: no WARN on stderr
PASS: base-override: exit code 0
PASS: base-override: git worktree add called with 'develop'
PASS: engine script has 3+ header comment lines
PASS: header includes What: line
PASS: header includes When: line
PASS: wt function is defined in .zshrc

Results: 25 passed, 0 failed
```

## Acceptance Criteria Verification

- [x] bats/bash tests cover all 4 resolution cases — 25 tests, all pass
- [x] Existing folder case prints 'attaching to existing branch <name>'
- [x] New branch case shows [Y/n] prompt before creating
- [x] Base override `wt feat/x develop` creates from develop
- [x] Missing feat/fix/spike prefix prints warning and continues
- [x] wt function is sourced and callable in a zsh session
- [x] Engine script includes 3-line header (name+one-liner, What:, When:)

## Closing Summary

Task fully implemented. Engine script at `.scripts/worktree` resolves 4 branch cases, delegates GX_BASE_BRANCH from gx-lib.sh, wt zsh function in .zshrc propagates cd to calling shell. 25 tests pass via plain bash test harness with PATH-injected mock git binary.
