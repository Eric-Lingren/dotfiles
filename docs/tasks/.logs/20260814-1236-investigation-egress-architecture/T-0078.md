# Task T-0078 — Build investigator orchestrator agent (Opus)

**Started:** 2026-08-14

## Objective

Create `agents/investigator.md` (Opus tier). The orchestrator decomposes a question into sub-claims, routes each to the correct source leaf agent, and aggregates per-leaf investigation-results into a unified top-level investigation-result. Enforces INSUFFICIENT_EVIDENCE propagation rule.

## Acceptance Criteria

- [ ] agents/investigator.md exists with Opus tier annotation
- [ ] Orchestrator decomposes a multi-source question and routes to at least two leaf agents
- [ ] Aggregated result is schema-valid investigation-result
- [ ] A sub-claim returning INSUFFICIENT_EVIDENCE causes aggregate to carry INSUFFICIENT_EVIDENCE, not a smoothed-over verdict
- [ ] Integration test verifies decompose -> route -> aggregate flow end-to-end

---

## TDD Cycle Results

### Files Created
- `claude-code-shared/agents/investigator.md` — Opus-tier orchestrator agent
- `claude-code-shared/scripts/tests/test_investigator_orchestrator.py` — 26 tests

### Files Modified
- `claude-code-shared/agents/registry.json` — added investigator entry (model: opus, consumers: investigate/pr-revise/pr-code-review)
- `claude-code-shared/skills/investigate/SKILL.md` — replaced stub with functional skill that spawns investigator agent

### Test Results
- 26/26 new tests GREEN
- 285/285 pre-existing passing tests still GREEN
- 15 pre-existing failures (debug attribution, inject_learning_tail, stop_hook) — unchanged, unrelated

### Vertical Slices
1. Agent file existence + Opus tier → create investigator.md
2. Routing to all 5 leaf agents + decomposition → add routing table + decomposition docs
3. INSUFFICIENT_EVIDENCE propagation rule + forbids smoothing → add propagation rule section
4. Schema-valid outputs, summary requirement → add output examples with self-validation
5. Registry entry → update registry.json
6. investigate skill not a stub → update SKILL.md
7. Integration: `_aggregate()` helper + 8 integration tests → 100% passing

## Runner Validation Gate

Tooling detection returned no formal manifest. Pure Python, tested directly with pytest.
- New tests: 26/26 PASSED
- Pre-existing passing: 285/285 PASSED
- Pre-existing failures: 15 unchanged (debug attribution tail, inject_learning_tail, stop_hook — unrelated)
- Gate decision: PASS

## Summary

T-0078 complete. Created `agents/investigator.md` (Opus tier) — orchestrator that decomposes questions, routes to 5 leaf agents, aggregates results, enforces INSUFFICIENT_EVIDENCE propagation. Updated registry.json and implemented the previously-stubbed investigate skill. 26 tests covering all acceptance criteria.
