# T-0074 — Build investigator-code leaf agent and delete vet stub

**Started:** 2026-08-14

## Context

- Create `agents/investigators/investigator-code.md` (Sonnet tier, Opus escalation watch)
- Agent accepts code-behavior claim, emits investigation-result contract
- Uses Grep/Glob/Read tools, evidence[] with ref=file:line format
- Write characterization tests for vet stub before deleting it
- Delete vet stub from skills/

## Pre-work investigation

- `agents/investigators/investigator-web.md` — reference implementation (Sonnet, web leaf agent)
- `contracts/investigation-result-schema.json` — schema v1
- `skills/vet/SKILL.md` — stub with diligence description, model: sonnet
- Existing tests: `scripts/tests/test_investigator_web.py`

## TDD cycle

### RED phase

Wrote `claude-code-shared/scripts/tests/test_investigator_code.py` with:
- 2 vet characterization tests (fail: vet still exists)
- 9 investigator-code structural tests (fail: agent doesn't exist)
- 7 schema-valid sample output tests (pass: schema-only, no agent dependency)

Run result: 3 failed, 7 passed, 8 errors (expected)

### GREEN phase

1. Created `claude-code-shared/agents/investigators/` directory
2. Created `claude-code-shared/agents/investigators/investigator-code.md`:
   - `model: sonnet` frontmatter
   - Opus escalation watch note (multi-file reasoning trigger)
   - tools: Grep, Glob, Read, Bash
   - Emits investigation-result schema v1
   - evidence[] with `source: "code"` and `ref: file:line` format
   - INSUFFICIENT_EVIDENCE fallback documented
   - Self-validation bash block
3. Deleted `claude-code-shared/skills/vet/` directory
4. Removed `vet` entry from `claude-code-shared/skill-pipeline.json`

Run result: 18/18 passed

### Cross-suite validation

Ran `test_skill_pipeline.py`, `test_investigator_web.py`, `test_investigation_result_schema.py`: 58/58 passed

### Runner validation gate

`detect_tooling.py` returned all nulls (dotfiles project — no configured lint/test/typecheck).
Validation satisfied by direct pytest runs: 18+58 = 76 total tests passing, 0 failed.

## Summary

Created investigator-code leaf agent (Sonnet tier, Opus escalation watch for multi-file reasoning). Agent emits investigation-result contract with code evidence using file:line refs. Wrote characterization tests documenting the vet stub's stated behavior, then deleted the vet stub and removed it from skill-pipeline.json. All 76 tests passing.
