# T-0083 — Build answer skill (thin Slack Q&A doorway)

**Start:** 2026-08-14
**Status:** In progress

---

## Goal

Create skills/answer.md (Haiku tier). Thin doorway skill: accepts a question, spawns investigator, passes result to answer-composer, returns composed Slack reply. No logic in skill body.

---

## Step 1: Check current state

Checked existing state. From the prior timed-out run:
- `claude-code-shared/skills/answer/SKILL.md` — already exists with Haiku tier, correct delegation body, learning-capture tail
- `claude-code-shared/resources/model-tiers.json` — `"answer": "T1"` already present
- `claude-code-shared/scripts/tests/test_answer_skill.py` — already exists with 17 tests
- `claude-code-shared/skill-pipeline.json` — `answer` NOT registered (the only missing piece)

## Step 2: TDD cycle

### RED

Ran tests before fix:
```
test_skill_pipeline_has_answer FAILED
```
16 passed, 1 failed.

### GREEN

Added `"answer": {"next": []}` to `skill-pipeline.json` (alongside the `investigate` terminal skill entry).

Ran tests after fix:
```
17 passed, 0 failed
```

### Runner validation

Tooling detection: no test/lint/typecheck commands configured for this workspace (pure markdown/Python project).

Ran `test_skill_pipeline.py` regression check: 18 passed, 0 failed.

Ran `test_inject_learning_tail.py`: 4 pre-existing failures confirmed (same failures with stash applied, not caused by this task's changes). Unrelated to T-0083.

## Closing Summary

Single change: added `"answer": {"next": []}` to `skill-pipeline.json`. The answer skill SKILL.md, model-tiers registration, test file, and learning-contract entry were all complete from the prior partial run. All 17 tests in `test_answer_skill.py` pass.
