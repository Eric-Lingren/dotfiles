# T-0079 — Build investigate skill (thin human doorway)

**Started:** 2026-08-14

## Goal

Create `skills/investigate.md` (Haiku tier). The skill is the human-facing doorway: it accepts a question, spawns the investigator orchestrator agent, and returns the raw investigation-result verdict to the caller. No verification logic lives in the skill itself — all diligence is in the agent layer. Register the skill so `/investigate` is invocable.

## Acceptance Criteria

- [ ] skills/investigate.md exists with Haiku tier annotation
- [ ] Skill spawns investigator agent and returns raw investigation-result
- [ ] No diligence or verification logic in the skill body
- [ ] Skill registered and invocable as /investigate

---

## Work Log

### RED phase

Wrote `claude-code-shared/scripts/tests/test_investigate_skill.py` (13 tests).

Initial run: 5 failed, 8 passed.

Failures:
- `test_investigate_skill_has_haiku_model` — had `model: sonnet`
- `test_investigate_skill_has_low_effort` — had `effort: high`
- `test_model_tiers_investigate_is_t1` — had `"investigate": "T3"`
- `test_investigate_skill_has_no_insufficient_evidence_logic` — body contained "INSUFFICIENT_EVIDENCE" output-handling text
- `test_investigate_skill_has_no_decomposition_instructions` — body had "What the investigator orchestrator does" section describing sub-claim decomposition

### GREEN phase

1. Updated `claude-code-shared/resources/model-tiers.json`: changed `"investigate": "T3"` → `"T1"`
2. Rewrote `claude-code-shared/skills/investigate/SKILL.md`:
   - Frontmatter: `model: haiku`, `effort: low`
   - Body: thin doorway only — spawns `investigator` agent, returns raw result
   - Removed: output formatting section, "What the investigator orchestrator does" section
3. Ran `sync-model-tiers.py --apply` — confirmed `ok investigate T1 -> haiku/low`

All 13 tests GREEN after changes.

### Full suite validation

Pre-existing failures (15) confirmed via `git stash` round-trip — all in unrelated test files:
- `test_debug_attribution_tail.py` (5)
- `test_inject_learning_tail.py` (4)
- `test_pr_code_review_attribution_tail.py` (5)
- `test_stop_hook.py` (1)

My 13 tests all pass. No regressions introduced.

### Files touched

- `claude-code-shared/skills/investigate/SKILL.md` — rewrote to thin Haiku-tier doorway
- `claude-code-shared/resources/model-tiers.json` — changed investigate from T3 to T1
- `claude-code-shared/scripts/tests/test_investigate_skill.py` — new (13 tests)

---

## Closing Summary

T-0079 complete. The investigate skill is now a Haiku-tier (T1) thin doorway: it accepts a user question, spawns the investigator orchestrator agent (Opus), and returns the raw investigation-result. No diligence, no verification logic, no sub-claim routing — all of that lives in the agent layer. The skill is registered in model-tiers.json and skill-pipeline.json and invocable as /investigate.
