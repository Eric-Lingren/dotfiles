# T-0075 — Build investigator-linear leaf agent

**Started:** 2026-08-14
**Task:** Create agents/investigators/investigator-linear.md (Sonnet tier)

---

## Context

Follows T-0072 (investigation-result schema + contract), T-0073 (investigator-web), T-0074 (investigator-code).

Pattern: leaf agent that accepts sub_claim, queries the relevant source, returns investigation-result JSON.

Linear MCP tools available: mcp__claude_ai_Linear tools.

Linear ticket URL format (from schema examples): `https://linear.app/<team>/issue/<ID>`

---

## TDD Cycle

### RED — test_investigator_linear.py written first (16 tests)
- 8 structural tests fail/error (agent file missing)
- 8 schema-only tests pass immediately (don't depend on agent file)
- Confirmed RED: `1 failed, 8 passed, 7 errors`

### GREEN — investigator-linear.md created
- frontmatter: model: sonnet, Linear MCP tools
- Contract section with self-validate command
- Process: search_issues → get_issue → list_comments → assign verdict → emit result
- source: "linear", ref: https://linear.app/<team>/issue/<ID>
- INSUFFICIENT_EVIDENCE with empty evidence[] when no record found

### Test results — GREEN
- `16 passed` for test_investigator_linear.py
- `74 passed` for full investigation pipeline (no regressions)
- Pre-existing failures in test_pr_code_review_attribution_tail.py (5) confirmed pre-existing on base branch — not caused by this task

### Runner validation
- Tooling detection: returned null for all (no pyproject.toml / pytest.ini found)
- Ran test suite directly via python3 -m pytest — 16 new tests pass, 74 investigation-pipeline tests pass

---

## Closing Summary

Created investigator-linear.md (Sonnet tier) following the established leaf-agent pattern. Agent accepts sub_claim, queries Linear via MCP tools (search_issues, get_issue, list_comments), emits investigation-result with source="linear" and Linear ticket URLs as refs. Returns INSUFFICIENT_EVIDENCE with empty evidence[] when no relevant record is found. 16 tests validate structural properties and schema compliance.

**Files touched:**
- claude-code-shared/agents/investigators/investigator-linear.md (created)
- claude-code-shared/scripts/tests/test_investigator_linear.py (created)
