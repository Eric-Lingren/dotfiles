# T-0070 — Update gxpush to append Closes #N to PR body

Started: 2026-08-13

## Goal
Extend gxpush to append 'Closes #N' to PR_BODY before calling `gh pr create`.
Two signals checked in order:
1. `$GITHUB_CLOSES` env var (explicit, bare number like "42")
2. `gh-[0-9]+` regex extraction from branch name (fallback)

Env var takes precedence when both present. When neither present, skip silently.

## Files to modify
- `/Users/eric/.dotfiles/.scripts/gxpush` (lines 270-281, PR creation block)

---

## Phase: RED — write failing tests

Wrote `.scripts/gxpush-issue-link.test.sh` with 7 test cases covering all 4 acceptance criteria signal combinations, plus edge cases (empty GITHUB_CLOSES, non-numeric gh- branch suffix, double-newline formatting).

Initial run had bash `set -e` + `((N++))` interactions causing early exit. Fixed by switching to `set -uo pipefail` and using `N=$((N+1))` arithmetic.

## Phase: GREEN — implement in gxpush

Modified `.scripts/gxpush` lines 270-294 (PR creation block). After `PR_BODY` and `BRANCH_SLUG` are set, added:
- Check `${GITHUB_CLOSES:-}` — if non-empty, use as issue number
- elif branch slug matches `gh-([0-9]+)` regex via `BASH_REMATCH` — extract number
- If issue number found: `PR_BODY="$(printf '%s\n\nCloses #%s' "$PR_BODY" "$_issue_number")"`
- Otherwise: skip silently

## Test results

```
Results: 10 passed, 0 failed
```

All 7 tests (10 assertions) pass. Tooling detection: no formal lint/test suite for dotfiles repo (bash workspace — expected).

## Files touched
- `.scripts/gxpush` (modified — added issue-linking block)
- `.scripts/gxpush-issue-link.test.sh` (new — bash test script)

