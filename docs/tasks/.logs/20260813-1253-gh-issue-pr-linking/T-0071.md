# T-0071 — Wire build-code SKILL.md and branching-strategy.md to encode gh-N

**Start:** 2026-08-13

## Task

Update build-code SKILL.md and branching-strategy.md so the AI encodes the GitHub issue number in the branch name and sets GITHUB_CLOSES before calling gxpush.

## Steps

### Read existing files

- Read `claude-code-shared/skills/build-code/SKILL.md` — identified step 4c as insertion point for export_url / gh-N logic.
- Read `claude-code-shared/resources/branching-strategy.md` — identified "Branch name derivation" section as insertion point.

### Edit branching-strategy.md

Updated "Branch name derivation (per-task)" section:
- Changed format line from `{prefix}/t-{id-number}-{kebab-title}` to `{prefix}/t-{id-number}[-gh-{issue}]-{kebab-title}`
- Added bullet explaining optional `gh-{issue}` segment, source (export_url last path segment), and position.
- Updated examples to show both without and with a GitHub issue number.
- Increased max chars note from ~40 to ~50 (gh-N segment adds characters).

### Edit build-code SKILL.md

Updated step 4c "Execute AFK tasks" point 2 to replace the single-line branch checkout with a multi-bullet block:
- Read `export_url` from task object.
- If GitHub issue URL: extract N, insert `gh-<N>` after task-ID segment, set `GITHUB_CLOSES=<N>` in environment.
- If null/absent/non-matching: skip silently.
- Create and check out derived branch.

## Closing summary

Both files updated. No automated test suite applies (documentation/instruction change). Acceptance criteria met:
1. SKILL.md documents export_url read, issue number extraction, gh-N encoding, GITHUB_CLOSES env var.
2. SKILL.md documents silent-skip when export_url is null.
3. branching-strategy.md shows optional gh-N segment with examples.
4. Smoke test described in acceptance criteria is an end-to-end user action (run build-code against a real task); the gxpush side was verified in T-0070.
