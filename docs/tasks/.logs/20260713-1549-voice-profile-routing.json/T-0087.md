# T-0087 — Create voice-routing.json router config

Start: 2026-07-13T23:05:56Z

## Task
Add `claude-code-shared/resources/voice-routing.json`, a new standalone router config
mirroring `model-tiers.json`'s two-level shape: a `profiles` map (profile name -> {file,
description}) and a `skills` map (skill name -> profile name, string-or-array value
for future multi-profile composability). Seed `profiles.dev-office` pointing at
`resources/voice-profiles/dev-office.md`, and seed `skills.pr-code-review` and
`skills.pr-revise` both routed to `dev-office`. Independent of `sync-model-tiers.py`.

## Approach

This is pure JSON config infra work in `claude-code-shared/`. Per project convention
(and explicit instruction from the caller), no unit-test suite is required for this
kind of change — the acceptance criteria are verification commands to run and report,
not a pytest/jest file to author. Skipped the standard `/tdd` red-green-refactor loop
and the lint-runner/test-runner validation gate accordingly; used direct verification
commands instead (see below), matching how `model-tiers.json` and `task-routing.json`
(its sibling router configs) are treated in this repo.

## Reference reading

- Read `claude-code-shared/resources/model-tiers.json` (ground truth for two-level
  map shape: `_doc`, definitions map, `default`, per-consumer maps).
- Read `claude-code-shared/resources/task-routing.json` (second reference for JSON
  style: `_doc` field, nested maps with `_meaning` sub-keys).
- Confirmed `claude-code-shared/resources/voice-profiles/dev-office.md` exists on disk
  (`ls -la`, 8.8k, created 13 Jul 15:07 in a prior session).

## File written

`claude-code-shared/resources/voice-routing.json`:

```json
{
  "_doc": "Router config mapping skills to voice profiles. 'profiles' maps profile name -> {file, description}, where 'file' is relative to resources/voice-profiles/. 'skills' maps skill name -> profile name; a value may also be an array of profile names for future multi-profile composability (not exercised today, but consumers must tolerate both string and array). Standalone config, independent of model-tiers.json and sync-model-tiers.py.",
  "profiles": {
    "dev-office": {
      "file": "dev-office.md",
      "description": "Default professional dev-office voice for PR review and revision output."
    }
  },
  "skills": {
    "pr-code-review": "dev-office",
    "pr-revise": "dev-office"
  }
}
```

## Verification commands run

1. `python3 -m json.tool claude-code-shared/resources/voice-routing.json`
   -> parsed cleanly, pretty-printed output matched expected structure. PASS.

2. `test -f claude-code-shared/resources/voice-profiles/dev-office.md && echo "dev-office.md exists"`
   -> `dev-office.md exists`. PASS. Confirms `profiles.dev-office.file` ("dev-office.md",
      relative to `resources/voice-profiles/`) resolves to a real file.

3. Python assertion script:
   ```python
   import json
   d = json.load(open('claude-code-shared/resources/voice-routing.json'))
   assert d['skills']['pr-code-review'] == 'dev-office'
   assert d['skills']['pr-revise'] == 'dev-office'
   assert d['profiles']['dev-office']['file'] == 'dev-office.md'
   print('all assertions passed')
   ```
   -> `all assertions passed`. PASS. Confirms all three acceptance criteria.

## Validation gate

Skipped lint-runner/test-runner spawn — no application code touched, single new
JSON file in `claude-code-shared/resources/`, no workspace test/lint tooling applies
to router config files of this kind (consistent with how `model-tiers.json` and
`task-routing.json` are maintained — hand-authored/reviewed, validated via
`json.tool`, no CI lint/test suite wired to them).

`git status --porcelain claude-code-shared/resources/voice-routing.json` confirmed
the new file is the only change introduced by this task (pre-existing unrelated
untracked/modified files in the working tree: `claude-code-shared/skills/pr-revise/SKILL.md`
(modified, unrelated prior work), plus other untracked docs/ files from prior sessions
— none touched by this task).

## Result

All three acceptance criteria verified via direct command output. No code review /
browser_verify required (task carries no `browser_verify` field).

End: 2026-07-13T23:07:30Z (approx)
