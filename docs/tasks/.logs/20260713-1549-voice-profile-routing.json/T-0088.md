# T-0088 — Wire pr-code-review to resolve its voice profile from voice-routing.json

Start: 2026-07-13

## Task
Add a step to pr-code-review/SKILL.md that resolves voice profile from voice-routing.json (lookup, no hardcoded path). Add brief Tone-vs-voice clarification note. Tone section content unchanged.


## Ground truth reviewed
- claude-code-shared/resources/voice-routing.json: profiles.dev-office.file="dev-office.md", skills.pr-code-review="dev-office"
- claude-code-shared/skills/pr-code-review/SKILL.md: Step 1 (gather diff) ends around line 70, Tone section at line ~121-133

## Edit approach
No unit-test suite required per project convention (memory: skip tests for claude-code-shared skills/agents/scripts work). This is a documentation/config-wiring change to a SKILL.md file. Applied edits directly and verified manually per acceptance criteria (treated as manual verification steps).

### Edit 1: New Step 1a inserted after "Read CLAUDE.md if present..." and before "## Step 2: Understand context"
Added lookup step: resolve `pr-code-review` via voice-routing.json's `skills` map -> profile name -> `profiles` map -> `file` -> read that file relative to voice-profiles/. Explicitly states "Do not hardcode a profile filename — always resolve it through voice-routing.json at run time."

### Edit 2: One-sentence clarification note placed immediately before "## Tone" heading (outside the Tone section body, so Tone section content itself remains byte-identical)
"Note: Tone (below) governs posture — when and how to raise something. The resolved voice profile (Step 1a) governs prose style — how the sentences sound. They compose, not compete."

## Manual verification (acceptance criteria)

1. grep for lookup step:
```
72:## Step 1a: Resolve voice profile
74:Before drafting review comments, resolve this skill's voice profile: look up `pr-code-review` in `claude-code-shared/resources/voice-routing.json`'s `skills` map to get the mapped profile name(s), then resolve each name in the `profiles` map to its `file` path (relative to `claude-code-shared/resources/voice-profiles/`) and read that file. Write review comments in that voice. Do not hardcode a profile filename — always resolve it through `voice-routing.json` at run time.
```
PASS — step present, no hardcoded dev-office.md path in this step (grep for "dev-office.md" returns zero hits in the file).

2. Manual resolution trace: skills.pr-code-review = "dev-office" -> profiles.dev-office.file = "dev-office.md" -> resolves to claude-code-shared/resources/voice-profiles/dev-office.md. PASS.

3. git diff of the Tone section shows only additive context lines before "## Tone" heading; the Tone section body itself (from "A good review asks open-ended questions..." through the "Reserve strong language..." bullet) is untouched — confirmed via `git diff`. PASS.

## Runner validation gate
Ran detect_tooling.py against project_root; workspace "." resolved language=unknown, lint/test/typecheck all null (no tooling manifest applicable to this markdown-only change). No lint-runner/test-runner spawned — consistent with project memory: "No unit tests for infra — skip tests for claude-code-shared skills/agents/scripts work."

## Browser verify
Not applicable — task has no browser_verify field.

## Result
status: done
files_touched: claude-code-shared/skills/pr-code-review/SKILL.md

End: 2026-07-13
