# T-0089 — Wire pr-revise to resolve its voice profile from voice-routing.json instead of a hardcoded path

Start: 2026-07-13

## Task
Replace hardcoded voice-profiles/dev-office.md path in pr-revise/SKILL.md Step 8b with a lookup through voice-routing.json's skills map.

## Verification
```
$ grep -n 'voice-profiles/dev-office.md' claude-code-shared/skills/pr-revise/SKILL.md
(no match -- hardcoded path successfully removed)

$ grep -n 'voice-routing.json' claude-code-shared/skills/pr-revise/SKILL.md
365:Before drafting, resolve this skill's voice profile: look up `pr-revise` in `claude-code-shared/resources/voice-routing.json`'s `skills` map to get the mapped profile name(s), then resolve each name in the `profiles` map to its `file` path (relative to `claude-code-shared/resources/voice-profiles/`) and read that file. Write every `reply_body` in that voice, not generic AI prose. Do not hardcode a profile filename — always resolve it through `voice-routing.json` at run time.
```

## Runner validation gate

Tooling detection result: single workspace "." with language "unknown", lint/format/typecheck/test/test_affected/e2e all null. No lint or test commands are configured for this dotfiles repo. Touched file (claude-code-shared/skills/pr-revise/SKILL.md) is documentation/prose (SKILL.md), not source code — no lint-runner/test-runner applicable. Per project convention (no unit-test suite required for skills/agents/scripts documentation work), this gate is a no-op pass.

## Manual acceptance criteria verification

1. "pr-revise/SKILL.md Step 8b no longer hardcodes the dev-office.md path; it looks up 'pr-revise' in voice-routing.json's skills map and resolves the profile file from there" — CONFIRMED. Old sentence removed, replaced with lookup-based sentence referencing voice-routing.json's `skills` map and `profiles` map.

2. "Manual check: running the lookup step for 'pr-revise' resolves to claude-code-shared/resources/voice-profiles/dev-office.md" — CONFIRMED by trace:
   - voice-routing.json `skills["pr-revise"]` = "dev-office"
   - voice-routing.json `profiles["dev-office"].file` = "dev-office.md"
   - Resolved path = claude-code-shared/resources/voice-profiles/dev-office.md ✓ (matches pr-code-review's identical mechanism, consistent).

3. "Reply-body quality bar's other instructions (register matching, grounding, first-person voice) are unchanged" — CONFIRMED. Only the "Before drafting, read..." sentence (line 365) was replaced; all subsequent bullets (register matching, grounding claims, first-person voice, no hedging, handle omission, per-class framing) are byte-identical to before.

No browser_verify present on this task — step 4 skipped.

## Closing summary

Edited claude-code-shared/skills/pr-revise/SKILL.md Step 8b: replaced the hardcoded `~/.dotfiles/claude-code-shared/resources/voice-profiles/dev-office.md` reference with a router-based lookup through voice-routing.json's `skills` and `profiles` maps, mirroring the mechanism already used by pr-code-review (SKILL.md line 74). All downstream instructions in section 8b are unchanged. No tests required (docs/config-wiring change, no tooling detected in repo). Status: done.

End: 2026-07-13
