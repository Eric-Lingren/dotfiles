# T-0090 — Extend register-skill's registration chain to assign voice-routing.json entries

Started: 2026-07-13

## Task
Add a new step to register-skill/SKILL.md's skill-registration path (alongside existing 2d/2e for model-tiers.json) that assigns a voice profile and writes it into voice-routing.json's skills map. Add a verify step analogous to 2f.


## Read: register-skill/SKILL.md

Key structure confirmed:
- Step 2c: "Skill path — producer enum upkeep" (conditional ask)
- Step 2d: "Skill path — edit model-tiers.json" — writes skill name into `skills` map at confirmed tier
- Step 2e: "Skill path — run sync" — runs sync-model-tiers.py --check/--apply
- Step 2f: "Skill path — verify" — three checks: tier-advisor resolves skill, frontmatter stamped, model-tiers.json consistent
- Step 2g: "Skill path — learning capture integration"

## Read: claude-code-shared/resources/voice-routing.json

Shape confirmed (built by T-0087):
```json
{
  "profiles": {
    "dev-office": { "file": "dev-office.md", "description": "..." }
  },
  "skills": {
    "pr-code-review": "dev-office",
    "pr-revise": "dev-office"
  }
}
```
Only one profile exists today: "dev-office".

## Plan

Insert new step "2d2. Skill path — assign voice-routing.json entry" right after 2d (before 2e), using a sub-letter to avoid renumbering the existing sequential steps (2e, 2f, 2g stay as-is). This step:
- Asks which voice profile to assign, listing profiles in voice-routing.json's `profiles` map, defaulting to "dev-office" when there's exactly one profile and skill is writing/review-style.
- Writes `"<skill-name>": "<profile-name>"` into voice-routing.json's `skills` map.
- Notes no analog to 2e (no sync script for voice-routing.json; read directly at runtime).

Extend step 2f's verify checklist with a 4th check: confirm skill's entry in voice-routing.json resolves to an existing profile in `profiles`, and that profile's `file` resolves to an existing file under resources/voice-profiles/.

No unit tests required per project convention (claude-code-shared skills/agents/scripts infra work). This is a documentation-only edit to SKILL.md. Verification is manual: grep for the new step/check, and dry-run reasoning against existing pr-code-review/pr-revise entries.

## TDD cycle

### RED

```
$ grep -n "2d2" claude-code-shared/skills/register-skill/SKILL.md
(no output, exit 1)
$ grep -n "voice-routing.json" claude-code-shared/skills/register-skill/SKILL.md
(no output, exit 1)
```
Confirmed: neither the new step nor any voice-routing.json reference exists yet. RED confirmed.

### GREEN

Inserted new step "2d2. Skill path — assign voice-routing.json entry" between 2d and 2e. Content:
- Reads voice-routing.json's profiles map, asks user which profile to assign (or defaults to the single profile for writing/review-style skills; skip for non-writing skills).
- Writes/overwrites the skill's entry in voice-routing.json's `skills` map.
- Explicitly notes no analog to 2e exists (no sync script; read directly at runtime).

Extended step 2f's verify checklist with a 4th check (python3 one-liner) confirming:
- skill has an entry in voice-routing.json's skills map
- the profile name is a key in profiles
- profiles.<name>.file resolves to an existing file under resources/voice-profiles/

```
$ grep -n "2d2" claude-code-shared/skills/register-skill/SKILL.md
184:### 2d2. Skill path — assign voice-routing.json entry
232:If step 2d2 assigned a voice profile, also run this fourth check:

$ grep -n "voice-routing.json" claude-code-shared/skills/register-skill/SKILL.md
(5 matches across step 2d2 and step 2f's new check 4)
```
GREEN confirmed.

Step sequence check (`grep -n "^### "`) shows well-formed ordering: ... 2d, 2d2, 2e, 2f, 2g, ... — no renumbering needed, consistent with the file's existing 1b/1c sub-lettering convention.

### Manual dry-run (acceptance criterion 3)

Ran the new verify-check python3 logic against the existing pr-code-review and pr-revise entries in voice-routing.json (written by T-0087/T-0088/T-0089, not re-registered):

```
$ python3 -c "... verify logic ..." for pr-code-review
OK: pr-code-review -> dev-office -> claude-code-shared/resources/voice-profiles/dev-office.md

$ python3 -c "... verify logic ..." for pr-revise
OK: pr-revise -> dev-office -> claude-code-shared/resources/voice-profiles/dev-office.md
```

Both pass. Confirms the new verify step's logic is correct, and that the assignment step is idempotent (writes/overwrites a single map key, no duplicate or broken entries would result from re-registering either skill).

### REFACTOR

No refactor needed — documentation-only change, wording already reviewed for clarity and consistency with the file's existing step style (mirrors 2d/2e/2f phrasing and structure).

## Runner validation gate

```
$ python3 ~/.dotfiles/claude-code-shared/scripts/tooling-detection/detect_tooling.py /Users/eric/.dotfiles
[
  {
    "workspace": ".",
    "language": "unknown",
    "lint": null, "format": null, "typecheck": null, "test": null, "test_affected": null, "e2e": null
  }
]
```

Tooling detection result: single workspace "." with language "unknown", all commands null. No lint or test tooling configured for this dotfiles repo.

```
$ git diff --name-only
claude-code-shared/skills/pr-code-review/SKILL.md   (T-0088, prior task)
claude-code-shared/skills/pr-revise/SKILL.md        (T-0089, prior task)
claude-code-shared/skills/register-skill/SKILL.md   (this task, T-0090)
```

Touched file for this task (register-skill/SKILL.md) is documentation/prose, not source code — no lint-runner/test-runner applicable. Per project convention (no unit-test suite required for claude-code-shared skills/agents/scripts documentation work — consistent with T-0087/T-0088/T-0089 precedent in this same run) and no tooling manifest present, this gate is a no-op pass.

## Browser verify

No `browser_verify` present on this task — step 4 skipped.

## Acceptance criteria — final confirmation

1. "register-skill/SKILL.md documents a new step that writes a skill's voice-routing.json skills-map entry during skill registration" — CONFIRMED (step 2d2, lines 184-201).
2. "The new step includes a verify check analogous to existing 2f, confirming the skill's entry in voice-routing.json resolves to an existing profile file" — CONFIRMED (step 2f's 4th check, lines 232-249).
3. "Manual check: re-registering pr-code-review or pr-revise following the new step resolves to an entry already matching dev-office (idempotent, no duplicate/broken entries)" — CONFIRMED via dry-run of the new verify-check logic against both existing entries; both resolve cleanly to dev-office -> dev-office.md, and the write step overwrites a single map key (no append), so re-registration is idempotent.

## Closing summary

Added step "2d2. Skill path — assign voice-routing.json entry" to claude-code-shared/skills/register-skill/SKILL.md, immediately after step 2d and before 2e, following the file's existing sub-lettering convention (1b/1c). The step asks the user which voice profile to assign (from voice-routing.json's profiles map, defaulting to the sole profile for writing/review-style skills), and writes/overwrites the skill's entry in voice-routing.json's skills map. Explicitly noted it has no analog to 2e (no sync script; consumed directly at runtime). Extended step 2f's verify checklist with a 4th check (python3 assertion script) confirming a skill's voice-routing.json entry resolves through profiles to an existing file under resources/voice-profiles/. Dry-run of the new verify logic against pr-code-review and pr-revise's existing dev-office entries (written in prior tasks this run) both pass, confirming correctness and idempotency. No automated tests required per established project convention for claude-code-shared documentation work; no tooling manifest configured in this repo. Status: done.

End: 2026-07-13
