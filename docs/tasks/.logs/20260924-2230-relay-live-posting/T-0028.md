# T-0028 — Add original comment and thread_database_id fields to schemas

Start: 2026-09-24

## Plan

Add three optional string fields to:
1. `claude-code-shared/contracts/seed-schema.json` — inside `provenance.items` properties
2. `claude-code-shared/contracts/task-schema.json` — inside `tasks` items properties

Fields:
- `original_comment_body` — text of the original review comment
- `original_comment_author` — author login of the original review comment
- `thread_database_id` — REST numeric comment id alongside the existing GraphQL node id

## Edits

### seed-schema.json — provenance.items properties
Adding three optional string fields after `reply_url`.

### task-schema.json — tasks items properties
Adding three optional string fields after `thread_id_type`.

## Validation

Running validate-schema.sh against both schemas.

### Results

seed-schema.json self-test (examples[]):
  OK: seed-schema.json: 1 example(s) valid

task-schema.json self-test (examples[]):
  OK: task-schema.json: 1 example(s) valid

seed instance validation:
  OK: 20260924-2223-relay-live-posting.json is valid against seed-schema.json

task instance validation:
  OK: 20260924-2230-relay-live-posting.json is valid against task-schema.json

All checks pass. Both schemas are backward compatible.

## Summary

Added original_comment_body, original_comment_author, thread_database_id as optional string fields
to seed-schema.json provenance.items and task-schema.json tasks.items. All validation passes.
