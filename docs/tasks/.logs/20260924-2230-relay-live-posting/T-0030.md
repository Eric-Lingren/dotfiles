# Task T-0030: Copy new provenance fields through to-tasks reply task generation

Start: 2026-09-24

## Goal

Update to-tasks SKILL.md Step 3c to copy `original_comment_body`, `original_comment_author`, and `thread_database_id` from each seed provenance item onto the corresponding reply task in the output JSON. Fields are optional — omit if absent from provenance item (backward compatible).

## Acceptance criteria

- [x] to-tasks Step 3c copies original_comment_body to reply tasks when present in provenance
- [x] to-tasks Step 3c copies original_comment_author to reply tasks when present in provenance
- [x] to-tasks Step 3c copies thread_database_id to reply tasks when present in provenance
- [x] Missing fields in provenance items do not cause errors (graceful omission — "copy from item if present; omit if absent")
- [x] Output task file validates against task-schema.json (schema updated in worktree to match shared dotfiles)

---

## Execution trace

### Files changed

- `claude-code-shared/skills/to-tasks/SKILL.md` — Added three new optional field lines to the reply task field list in Step 3c (after `thread_id` + `thread_id_type`)
- `claude-code-shared/contracts/task-schema.json` — Added `original_comment_body`, `original_comment_author`, `thread_database_id` properties (worktree was behind T-0028's changes which had been applied to shared dotfiles but not this worktree branch)

### Edit in SKILL.md (Step 3c reply task fields, lines 217-219)

Added after the existing `thread_id` + `thread_id_type` line:

```
  - `original_comment_body`: copy from the item if present; omit if absent
  - `original_comment_author`: copy from the item if present; omit if absent
  - `thread_database_id`: copy from the item if present; omit if absent
```

### Edit in task-schema.json

Added three property definitions after `thread_id_type` in the reply task's properties block. JSON validated successfully.

### Tooling note

Tooling manifest shows no lint/format/typecheck/test commands for this workspace (markdown + JSON only). No runners to spawn.

---

## Closing summary

Step 3c in to-tasks SKILL.md now documents the three new optional provenance fields (`original_comment_body`, `original_comment_author`, `thread_database_id`) with explicit "copy if present; omit if absent" semantics, making them backward compatible. task-schema.json in the worktree was also updated to include these fields (the shared dotfiles had them from T-0028, but this worktree branch was behind).
